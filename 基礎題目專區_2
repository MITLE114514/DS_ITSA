#pragma warning( disable: 6031 4996 )
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    double input;
    while(scanf("%lf", &input) != EOF) {
        printf("%.1lf\n", input * 1.6);
    }
}
